<?php include_once("admin/header.phtml"); ?>
<?php include_once("admin/breadcrumb.phtml"); 
	$product = $Contents['results'][0];
	$product_fields = $Contents['fields'];
	$custom = array();
	foreach($product_fields as $field) {
		if($field[0]->custom == 1) {
			$custom[] = $field[0];
		}
	}
	$image = $product->content_image ? "/" . PROJECT_DIRECTORY . $product->content_image : FRONT_IMG_PATH . "default-image.png";
?>            
	<div id="main-wrapper">
		<div class="row">
			<div class="col-md-12">
                <div class="panel panel-white">
                    <div class="panel-heading clearfix">
                        <h4 class="panel-title ">
                        	<?= $product->content_title; ?>
                        </h4>
                    </div>
					<div class="panel-body">
					<div class="col-md-6 product_image"><img src="<?= $image ?>"></div>
					<div class="col-md-6">						
					
						<h3>Attributs</h3> 
						<div class="col-md-3">
                        	<b>Prix :</b>
                        </div>	
						<div class="col-md-9">
							<?= $product->content_price; ?> €
                        </div>

						<div class="col-md-3">
                        	<b>Catégorie :</b>
                        </div>	
						<div class="col-md-9">
							<?= $product->category_name; ?>
                        </div>

						<div class="col-md-12">
                        	<b>Description :</b>
                        </div>	
						<div class="col-md-12">
							<?= $product->content_description; ?>
                        </div>

						<?php if(!empty($custom)) { ?>
							<h3>Champs supplémentaire(s)</h3> 
							<?php foreach($custom as $c) { ?>
	                        	<?= '<div class="col-md-3"><b>'.$c->label .' :</b></div><div class="col-md-9"> '.$product->{str_replace('field_','content_',$c->name)}.'</div>'; ?>
	                        <?php } ?>
	                     
	                    <?php } ?> 
	                    <p>
		                    <a href="<?= '/' . PROJECT_DIRECTORY . 'dashboard/product/edit/' . $product->content_id ?>">Modifier ce contenu.</a>
		                </p>  	
			          	</div>
					</div>
                </div>
			</div>
        </div>
    </div>
<?php include_once("footer.phtml"); ?>